it has a native bundler to replace tools like webpack and also has a native transpiler so you can write typescript code out-of-the-box with cool features like top-level away and I did it already been pioneered by Dino but bun will also transpile your JS X-Files poster like do you know it prioritizes web apis like that while at the same time also supporting many nodecore modules as well as note ATI which will allow many npm package has to also working but in fact it Implement knows module resolution algorithm which means you can install packages from NDM into bun and the packages install 20 times faster it feels like magic another nice ergonomic future is that environment variables load automatically it's not like nodejs where you have to install